info:
  name: Match Job
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/match"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
