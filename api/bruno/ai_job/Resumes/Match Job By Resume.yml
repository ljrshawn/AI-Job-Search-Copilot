info:
  name: Match Job By Resume
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/match/3"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
