info:
  name: Login
  type: http

http:
  method: GET
  url: "{{URL}}/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@shawnl.dev",
        "password": "Test123$"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​