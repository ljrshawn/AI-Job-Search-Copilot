info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/users/"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "email": "admin@shawnl.dev",
        "first_name": "Shawn",
        "last_name": "L",
        "password": "Test123$"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
